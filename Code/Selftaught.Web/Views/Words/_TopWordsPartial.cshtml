@using Selftaught.Web.ViewModels.Words
@using System.Collections.Generic
@model IEnumerable<WordDetailedViewModel>

@{
    var firstColCount = Model.Count() / 2;
    var leftColElems = Model.Take(firstColCount);
    var rightColElems = Model.Skip(firstColCount);
}

<div class="panel panel-primary">
    <div class="panel-heading">
        <h3 class="panel-title">Most mistaken words</h3>
    </div>
    <div class="panel-body">
        <div class="row">

            @if (@Model.Any() == false)
            {
                <span>There are no words in this language</span>
            }
            else
            {
                <div class="col-md-6">
                    @foreach (var elem in leftColElems)
                    {
                        @elem.Translations.First().Meaning <br />
                    }
                </div>
                
                <div class="col-md-6">
                    @foreach (var elem in rightColElems)
                    {
                        @elem.Translations.First().Meaning <br />
                    }
                </div>
            }
        </div>
    </div>
</div>